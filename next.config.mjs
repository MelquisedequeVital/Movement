/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['Movement.com'], // Adapte conforme necessário
    },
};



export default nextConfig;
